# Demo Application - Managed by ArgoCD
# Uses multi-source to load values from a different path than the chart

apiVersion: argoproj.io/v1alpha1
kind: Application
metadata:
  name: demo-app
  namespace: argocd
  finalizers:
    - resources-finalizer.argocd.argoproj.io
spec:
  project: default

  sources:
    # Source 1: Helm chart
    - repoURL: https://github.com/shafivullashaik-tech-ops/devops-portfolio.git
      targetRevision: main
      path: app-microservice-demo/helm
      helm:
        valueFiles:
          - $values/gitops-eks-platform/environments/dev/values.yaml

    # Source 2: Values file from repo root (referenced as $values)
    - repoURL: https://github.com/shafivullashaik-tech-ops/devops-portfolio.git
      targetRevision: main
      ref: values

  destination:
    server: https://kubernetes.default.svc
    namespace: default

  syncPolicy:
    automated:
      prune: true
      selfHeal: true
    syncOptions:
      - CreateNamespace=true

  # Health assessment
  ignoreDifferences:
    - group: apps
      kind: Deployment
      jsonPointers:
        - /spec/replicas  # Ignore replica changes from HPA
